# Required for GPU workloads on k3s when NVIDIA Container Toolkit is installed.
# k3s auto-registers the "nvidia" containerd runtime at startup if the toolkit is present.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
